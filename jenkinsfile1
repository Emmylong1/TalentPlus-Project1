pipeline {
  agent {label 'linux'}
  
  stages {
    stage ('Talentplus') {
      steps {
         sh '''
           ansible --version
           ansible-playbook-version
           ansible-galaxy --version
         '''
         }
       }
    }
 }
      
